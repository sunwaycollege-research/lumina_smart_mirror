{
  "name": "lumina-premium",
  "version": "1.0.0",
  "description": "Lumina Premium — Executive Smart Mirror",
  "private": true,
  "scripts": {
    "install:all": "npm install && pip install -r requirements.txt --break-system-packages",
    "enroll":      "python3 modules/LuminaFaceID/enroll_user.py",
    "start":       "cd /path/to/MagicMirror && node --run start"
  },
  "dependencies": {
    "node-ical"  : "^0.20.0",
    "ws"         : "^8.17.0",
    "express"    : "^4.18.2",
    "qrcode"     : "^1.5.3"
  }
}
